package jif.util;

class AbstractMap$AbstractSet[label K, label V] extends AbstractSet[K]() {
    final private AbstractMap parent;
    AbstractMap$AbstractSet(AbstractMap parent)
    {
      this.parent = parent;
    }
		public Iterator[K] iterator() {
		    return new AbstractMap$AbstractSet$Iterator[K,V](this);
		}

    public int size() {
		    return AbstractMap.this.size();
		}

		public boolean contains(Object k) {
		    return AbstractMap.this.containsKey(k);
		}
}
