package jif.lang;

/**
 * This interface combines the Jif signatures for the methods
 * equals(), hashCode() and toString(). This provides a 
 * convenient replacement for 
 */
public interface JifObject[covariant label L] extends IDComparable[L], 
					    Hashable[L], 
					    ToStringable[L] 
{

}
