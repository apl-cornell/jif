class Outer {
    Member foo() {
	return this.new Member();
    }
    Member bar() {
	return new Member();
    }
    class Member { }
}

class Outer2[label L1, label L2] {
    Outer.Member{L1} foo{L1}(Outer{L2} a) {
       if (a != null) {
	    return a.new Member();
       } else return null;
    }
}
