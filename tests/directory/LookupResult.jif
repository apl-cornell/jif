class LookupResult {
    final label{this} label_; // XXX decl removed in Fabric version
	final Object{*label_; this} obj;

    LookupResult(label{this} l, Object{*l; this} o) {
		label_ = l;
        obj = o;
    }
}

// vim: ts=4 sw=4
