
/**
 * This class demonstrates an unsoundness in the assumptions made about setting
 * final fields.
 */

class UnsoundConstructor {
	public static boolean {_→} _declassify (boolean {*→} y) {
		UnsoundConstructor o;
		if (y)
			o = new UnsoundConstructor(0);
		else
			o = new UnsoundConstructor();
		return o.x;
	}



	final boolean {_→} x;

	UnsoundConstructor {*→} ()      { x = false; }
	UnsoundConstructor {*→} (int a) { x = true;  }

}
		
